# Use root/example as user/password credentials
version: '3.1'
services:
  app:
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nestjsBackend
    ports:
      - '443:3000'
    restart: always
